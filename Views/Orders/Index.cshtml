@model IEnumerable<Core.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-controller="Shopping" asp-action="">Create New Order</a>
</p>

You can change certain details of your order up until its status changes to dispatched. Once an order is dispatched it cannot be changed.  If you have any problems with your orders please call our support line.
<table class="table">
    <thead>
        <tr>
                
                <th>
                    @Html.DisplayNameFor(model => model.OrderDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DeliveryDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.GeocodedAddress)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Total)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Details)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SpecialInstructions)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Status)
                </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
           
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GeocodedAddress)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SpecialInstructions)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            
            <td>
                @{if (item.Status==5){
                        <span>On Route</span>
                        <!--<a asp-action=Edit asp-route-id=@item.ID>Edit</a> -->
                    }else if(item.Status==10){ 
                        <span>Delivered</span>
                    }else{
                        <span>Sending...</span>
                    }
                    
                }

                <a asp-action="Details" asp-route-id="@item.ID">Details</a> 
                <a asp-action="" asp-controller="MapDelivery" asp-route-id="@item.ID">Map</a>
            </td>
        </tr>
}
    </tbody>
</table>






